
def get_query_param_name(index: int) -> str:
    """
    Gets the query parameter name

    :param index: the index to use as part of the parameter name
    :return: the parameter name
    """
    return f"param_{index}"
